# GitHub Pages (docs/)

Este site é publicado via GitHub Pages usando a pasta **/docs**.

Arquivos de dados gerados pelo workflow:
- `docs/api/*.json` (último resultado + bundle)
- `docs/data/history_*.json` (histórico incremental)

